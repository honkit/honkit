// prettier.config.mjs
// Prettier also enforces some conditions in .editorconfig.

/**
 * @see https://prettier.io/docs/en/configuration.html
 * @type {import("prettier").Config}
 */
const config = {
    singleQuote: false,
    trailingComma: "none"
};

export default config;
